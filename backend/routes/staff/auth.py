from flask import Blueprint, request, make_response, jsonify
from datetime import datetime

auth_blueprint = Blueprint('staff_auth', __name__, url_prefix="/staff/auth")

# TODO: Staff Register
# TODO: Staff Login
